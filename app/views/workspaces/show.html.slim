.ui-layout-west.pane
  = render 'workspaces/pane_left'
.ui-layout-center.pane
  = render 'workspaces/canvas_toolbar'
  #canvas-container
    #canvas

.ui-layout-east.pane
  = render 'workspaces/pane_right'

- if @action
  javascript:
    _action = "#{@action}"
    _pid = "#{@pid}"
